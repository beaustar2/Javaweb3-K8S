apiVersion: v1
kind: Pod
metadata:
  name: javaweb3-k8s-pod
spec:
  containers:
  - name: web
    image: httpd
    ports:
    - containerPort: 80
    resources:
      limits:
        cpu: "0.5"
        memory: "512Mi"
      requests:
        cpu: "0.2"
        memory: "256Mi"